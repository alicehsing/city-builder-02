export function makeStatsString(skylineCount, waterfrontCount, castleCount) {
    return `You have clicked on Skyline ${skylineCount} times.
    You have clicked on Waterfront ${waterfrontCount} times.
    You have clicked on Castle ${castleCount} times.`;
}